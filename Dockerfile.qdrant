FROM qdrant/qdrant

# Install curl for health checks
USER root
RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*

# Switch back to the original user (qdrant containers typically run as root anyway)
USER root

